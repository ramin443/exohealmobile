import 'package:exohealandroid/constants/fontconstants.dart';
import 'package:flutter/material.dart';

/*ThemeData getAppTheme(BuildContext context, bool isDarkTheme) {
  double screenwidth = MediaQuery.sizeOf(context).width;
  return ThemeData(
    scaffoldBackgroundColor: isDarkTheme ? Colors.black : Colors.white,
    textTheme: Theme.of(context)
        .textTheme
        .copyWith(
      titleLarge: , //this is for row main title
      titleMedium: , //this is for row view more text
      titleSmall: , //this is for description within a row

      headline4: , //this is for text within
      bodyText1:
      Theme.of(context).textTheme.titleSmall?.copyWith(
          fontFamily: intermedium,
          color: Color(0xffA8A8A8),
          fontSize: screenwidth * 0.0293),

      bodyText2: Theme.of(context).textTheme.titleSmall?.copyWith(
          fontFamily: intermedium,
          color: Color(0xffDDDDDD),
          fontSize: screenwidth * 0.0293),
    )
        .apply(
      bodyColor: isDarkTheme ? Colors.white : Colors.black,
      displayColor: Colors.grey,
    ),
    switchTheme: SwitchThemeData(
      thumbColor: MaterialStateProperty.all(
          isDarkTheme ? Colors.orange : Colors.purple),
    ),
    listTileTheme: ListTileThemeData(
        iconColor: isDarkTheme ? Colors.orange : Colors.purple),
    appBarTheme: AppBarTheme(
        backgroundColor: isDarkTheme ? Colors.black : Colors.white,
        iconTheme:
        IconThemeData(color: isDarkTheme ? Colors.white : Colors.black54)),
  );
}*/